#include <stdio.h>
#include<string.h>

int main()
{
    char p[100];
    printf("enter your character");
    scanf("%s",p);
    int n=strlen(p),i;
    for(i=0;i<n;i++)
    {
       if(p[i]=='a')
       {
           p[i]='A';

       }

    }
    printf("%s",p);

    return 0;
}
